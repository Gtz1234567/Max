const { EmbedBuilder } = require("discord.js");

module.exports = {
    name: "transactions",
    aliases: ["transações"],
    
    category: '「 Economia 」',
    usage: 'transactions',
    permissions: ['Nenhuma.'],
    description: 'Exibir transações recentes.',

    run: async (client, message) => {
        
    }
};